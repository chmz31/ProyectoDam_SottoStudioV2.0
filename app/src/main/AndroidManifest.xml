<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SottoStudio"
        tools:targetApi="31">
        <!-- MainActivity configurada como pantalla principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de la actividad InicioSesion -->
        <activity
            android:name=".InicioSesion"
            android:exported="false" />

        <!-- Declaración de la actividad Registro (si es necesaria) -->
        <activity
            android:name=".Registro"
            android:exported="false" />

        <!-- Declaración de otras actividades (si las hay) -->
        <activity
            android:name=".PerfilAlumno"
            android:exported="false" />
        <activity
            android:name=".PerfilProfesor"
            android:exported="false" />

    </application>

</manifest>
